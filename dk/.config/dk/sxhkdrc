# launcher
super + {d, x, w}
	rofi -show {drun,run -no-show-icons,window}

super + e
	rofi -modi "emoji:rofimoji" -show emoji -no-show-icons

# dropdown terminal
alt + { F2, F3 }
	~/bin/scratchpad.sh { dropdown, musikcube }

alt + F1
	~/bin/kitty-launch.sh pulsemixer

alt + F4
  ~/bin/rofi/powermenu.sh

# system monitor
ctrl + alt + Delete
	~/bin/kitty-launch.sh btop

# color picker
alt + c
	gpick -p

# toggle polybar
ctrl + F1
	polybar-msg cmd toggle

# terminal
super + Return
	kitty

# screenshot and selection capture
{_,alt +, ctrl +, shift + }@Print
	~/bin/rofi/scrotting.sh {_, screen, area, window}

# dedicated volume keys
{XF86AudioRaiseVolume,XF86AudioLowerVolume}
	amixer set Master {5%+, 5%-}

# toggle mute for output input devices
{XF86AudioMute, XF86AudioMicMute}
	amixer set {Master, Capture} toggle

# dedicated backlight keys
{XF86MonBrightnessUp,XF86MonBrightnessDown}
	xbacklight {+10,-10}

# reload sxhkd
super + Escape
	pkill -USR1 -x sxhkd -c "$HOME/.config/dk/sxhkdrc"

# restart/reload dk
{super, alt} + shift + r
	dkcmd {restart, reload}

# focus next or previous window
super + {k,j}
	dkcmd win focus {next,prev}

# close window, swap tiled window in/out of master, cycle tiled windows in place
super + {q,space,Tab}
	dkcmd win {kill,swap,cycle}

# toggle fullscreen and fake fullscreen (enable manipulating fullscreen window)
{_,alt + }F11
	dkcmd win {full,fakefull}

# toggle floating or sticky
alt + {f, shift + s}
	dkcmd win {float,stick}

# move window, signed (+/-) for relative changes
ctrl + shift + alt + {h,j,k,l}
	dkcmd win resize {x=-20,y=+20,y=-20,x=+20}

# resize window, signed (+/-) for relative changes
alt + ctrl + {h,j,k,l}
	dkcmd win resize {w=-20,h=+20,h=-20,w=+20}

# move the active window up/down the stack
super + shift + {k,j}
	dkcmd win mvstack {up,down}

# view, send, or follow to a workspace (by number)
super + {_,shift + ,ctrl + }{1-9,0}
	dkcmd ws {view,send,follow} {1-9,10}

# view, send, or follow to the next, previous, last active,
alt + {_,shift + ,ctrl + }{bracketleft,bracketright,BackSpace}
	dkcmd ws {view,send,follow} {prev,next,last}

# next non-empty, or prev non-empty workspace
super + {h,l}
	dkcmd ws {prevne,nextne}

# view, send, or follow to the next, previous, or last active monitor
alt + {_,shift + ,ctrl + }{comma,period,backslash}
	dkcmd mon {view,send,follow} {prev,next,last}

# change active workspace layout
alt + {t,r,m,g,s,w,f,u}
	dkcmd set layout {tile,rtile,mono,grid,spiral,dwindle,none,tstack}

# change number of windows in master or first stack
alt + {_,shift + }{i,d}
	dkcmd set {master,stack} {+1,-1}

# change gap width
alt + {equal,minus}
	dkcmd set gap width {+5,-5}

# change border widths
alt + ctrl + {_,shift + }{equal,minus}
	dkcmd set border {width,outer_width} {+1,-1}
